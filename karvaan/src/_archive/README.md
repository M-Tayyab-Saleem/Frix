# src/_archive/

This directory holds **deprecated navigation and screen files** from the Karvaan
app that predate the Frix AI Service Orchestrator rewrite.

## Files archived at Phase 1 migration
- `MainNavigator.tsx` — 5-tab legacy navigator (superseded by 4-tab MainTabNavigator)
- `ShareLinkNavigator.tsx` — legacy deep-link navigator (not used in orchestrator shell)
- `AuthNavigator.tsx` — Supabase auth flow (removed for hackathon — no auth required)

These files are kept for reference only. Do not import from this directory.
